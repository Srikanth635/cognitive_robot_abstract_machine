[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "uniworld"
version = "0.1.0"
description = "World setup utilities for simulation environments"
requires-python = ">=3.10,<3.13"

[tool.setuptools.packages.find]
where = ["src"]
